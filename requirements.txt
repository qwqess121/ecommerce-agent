# 电商客服智能体 — 部署依赖（版本已本地验证锁定，确保 Render 上可复现）
# 说明：默认无 LLM_API_KEY 时自动进入 Mock 演示模式，离线即可回答，无需任何密钥。
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
httpx==0.28.1
python-dotenv==1.2.2

# 检索 / 向量
chromadb==1.5.9
rank-bm25==0.2.2
openai==2.46.0

# LangChain 生态（真正的 LangChain：Chroma + EnsembleRetriever + LCEL + LangGraph）
langchain==1.3.14
langchain-core
langchain-text-splitters==1.1.2
langchain-openai==1.3.5
langchain-community==0.4.2
langchain-chroma==1.1.0
langchain-classic==1.0.8
langgraph==1.2.9
